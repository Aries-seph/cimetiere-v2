def get_caveaux():
    try:
        response = httpx.get(
            f"{BASE_URL}/caveaux/",
            headers=api_client.get_headers(),
            timeout=60.0
        )
        return response.json()
    except Exception:
        return None